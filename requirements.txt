# Core dependencies
PyQt6>=6.6.0
python-vlc>=3.0.18
mutagen>=1.47.0
structlog>=24.1.0

# Development
py2app>=0.28.0
pytest>=7.0.0
# Linting/formatting: ruff is exact-pinned because `ruff format --check` is a
# blocking CI gate and formatter output can change between releases. Bump
# deliberately in its own commit. mypy is advisory-only, so a floor is fine.
ruff==0.15.20
mypy>=1.13
