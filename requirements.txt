# Dependencies pinned to exact versions for reproducible installs and
# deliberate (not automatic) upgrades. These are the versions the test suite
# is verified against. Bump intentionally — ideally via Dependabot PRs, which
# CI then validates. Note: this pins direct deps only; transitive deps (e.g.
# PyQt6-Qt6) still resolve freshly. A full lockfile (pip-tools/uv) would close
# that gap if needed later.

# Core (runtime)
PyQt6==6.10.2
python-vlc==3.0.21203
mutagen==1.47.0
structlog==25.5.0

# Development / CI
# Each platform installs only its own app builder: py2app makes the macOS
# .app (setup.py), PyInstaller the Windows build (windows.spec, CI-only).
py2app==0.28.9; sys_platform == "darwin"
pyinstaller==6.22.3; sys_platform == "win32"
pytest==9.0.3
ruff==0.15.20
mypy==2.1.0
